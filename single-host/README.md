### Certains precautions
```
# Create customized jenkins image
sudo mkdir -p /var/jenkins_home
sudo mkdir -p /var/postgres-data

sudo chown <user> /var/jenkins_home
sudo chown <user> /var/postgres-data
```
